<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.UserEntityMapper1">
    <!-- 解决表名与字段不匹配 -->
    <resultMap type="com.test.entity.UserEntity1" id="BaseResultMap">
        <id property="id" column="USER_ID"/>
        <result property="userName" column="USER_NAME"/>
        <result property="userPwd" column="USER_PWD"/>
    </resultMap>

    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select * from  USERINFO
        WHERE  USER_ID = #{id}
    </select>

    <select id="selectByEntity" parameterType="com.test.entity.UserEntity1" resultMap="BaseResultMap">
        select * from  USERINFO
        WHERE  USER_NAME = #{userName} AND USER_PWD = #{userPwd}
    </select>

    <insert id="insertUser" parameterType="com.test.entity.UserEntity1">
        insert into USERINFO (USER_NAME,USER_PWD) values (#{userName},#{userPwd});
    </insert>

</mapper>
